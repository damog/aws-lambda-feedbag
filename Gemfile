source "https://rubygems.org"

# For Lambda deployment (Ruby 3.4), use Docker which will get the latest compatible versions
# For local development, these versions work with Ruby 2.6+
gem "nokogiri"  # Let bundler pick the right version for your Ruby
gem "feedbag", "~> 1.0"  # Latest version
